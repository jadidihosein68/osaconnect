DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=*

# Database
# Example: postgres://username:password@hostname:5432/dbname
DATABASE_URL=postgres://postgres:admin@localhost:5432/osaconnect

CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0
ASSISTANT_KB_PATH=knowledge_base.md
OUTBOUND_PER_MINUTE_LIMIT=60
CALENDAR_PROVIDER=google
CALENDAR_API_KEY=change-me
ASSISTANT_PROVIDER=openai
ASSISTANT_API_KEY=change-me
FERNET_KEY=generate-with-`python - <<'PY'\nfrom cryptography.fernet import Fernet; print(Fernet.generate_key().decode())\nPY`
ALERTS_EMAIL_TO=
ELEVENLABS_BASE_URL=https://api.elevenlabs.io
# ElevenLabs secrets are stored per org via /api/integrations/elevenlabs/connect/
# Sample placeholders (do not commit real keys):
# ELEVENLABS_API_KEY=
# ELEVENLABS_AGENT_ID=
# ELEVENLABS_AGENT_PHONE_NUMBER_ID=
# ELEVENLABS_WEBHOOK_SECRET=
# ELEVENLABS_TEST_TO_NUMBER=
UNSUBSCRIBE_URL=http://localhost:8000   # base URL used to generate unsubscribe links (defaults to SITE_URL if empty) point to backend
UNSUBSCRIBE_MAILTO=unsubscribe@example.com
EMAIL_FOOTER_TEXT=If you no longer wish to receive these emails, you can unsubscribe below.
EMAIL_BATCH_SIZE=100
EMAIL_BATCH_DELAY_SECONDS=1
EMAIL_RETRY_DELAY_SECONDS=10
EMAIL_MAX_RETRIES=2
MEDIA_ROOT=media
MEDIA_EXTERNAL_BASE_URL=https://suzann-inexpugnable-laylah.ngrok-free.dev
